spring:
  application:
    name: my-spring-app  # 服務名稱，會在 Nacos 註冊
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848  # Nacos 服務註冊中心地址
      config:
        server-addr: localhost:8848  # Nacos 配置中心地址
        file-extension: yaml  # 默認讀取的配置格式
